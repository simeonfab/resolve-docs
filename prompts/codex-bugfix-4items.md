---
status: historical
---

Repo: simeonfab/ResolveOS-intake-frontend, branch: main, current commit: 80e9f40

Frontend fixes only. Do not touch api/resolve.js unless explicitly stated. Do not touch anything not listed. Ask rather than guess if context is missing.

================================================================================
FIX 1 - Tool chip "+ Add" expansion is broken and overlaps the circle border
================================================================================

When the user taps "+ Add" in the tool chip row on Screen 2, the chip should smoothly transform inline into a text input. Currently the animation is poor and the text box clips/overlaps the chip's border circle.

Fix:
- The "+ Add" chip expands smoothly in place into an inline text input. The chip itself becomes the input — no new element appearing elsewhere, no overlap with adjacent chips or borders.
- Use a CSS width/opacity transition so it feels like the chip is opening up, not a new element appearing
- Input should have a clear border matching the app's existing input style (amber focus border)
- On Enter or blur (if non-empty), the typed text becomes a new chip in the row, and the "+ Add" chip reappears at the end
- On blur (if empty), the input collapses back to "+ Add" with no chip added
- The chip row should never overflow or clip — use flex-wrap if needed so chips wrap cleanly to a new line if there are many

================================================================================
FIX 2 - Gap questions logic: remove hardcoded "Two things" and fix question quality
================================================================================

The section heading "Two things Resolve still needs" hardcodes the number and implies there will always be exactly two questions. The questions being generated are also not appropriate — they're asking deep strategic business questions rather than gaps that actually block producing a good recommendation.

Two parts to this fix:

PART A - Frontend (public/index.html):
- Remove "Two things Resolve still needs" as a static heading
- Replace with a dynamic heading driven by however many gap questions actually come back from Call 1:
  - 0 questions: don't show this section at all — replace with a brief positive confirmation like "Resolve has everything it needs to build your path."
  - 1 question: heading "One thing Resolve still needs"
  - 2 questions: heading "A couple of things Resolve still needs"
  - The section should only appear if there are genuine gap questions to ask

PART B - api/resolve.js (Call 1 system prompt only):
The gapQuestions generated by Call 1 are currently off-target. Add the following explicit constraint to the Call 1 system prompt, clearly separated from the existing instructions:

"CRITICAL RULE FOR GAP QUESTIONS: Gap questions must only ask for information that is DIRECTLY needed to produce a clear, grounded project recommendation and execution path. Do not ask strategic business questions, market questions, pricing questions, or anything that goes beyond what's needed to understand this project's current state and immediate next step. Valid gap questions are things like: 'What have you already tried?' or 'Is there a deadline driving this?' or 'Who else is involved in this project?' Invalid gap questions are things like: 'What should your first paid offer be?' or 'What assets can be used commercially?' — these are business strategy questions, not intake blockers. If you genuinely have everything needed to produce a good recommendation from the input provided, return an empty gapQuestions array. Do not pad with questions just to fill the two slots."

================================================================================
FIX 3 - Placeholder text in gap question answer boxes
================================================================================

The answer input boxes under each gap question currently show example placeholder text (e.g. "e.g. ..."). Since questions are dynamic, the placeholder can't be specific.

Fix: replace all gap question input placeholders with simply "Type your answer here" — nothing more specific than that.

================================================================================
FIX 4 - "Done" button on final screen does nothing
================================================================================

The last screen currently has a "Done" button that does nothing when clicked.

Fix: replace "Done" with "Start over" — clicking it resets the entire flow back to Screen 0's first beat (name entry), clears all session state (userName, understandingData, recommendationData, confirmedTools, gap answers, all inputs), and resets the progress bar. This makes retesting easy without requiring a full page reload.

================================================================================
After fixing all 4: git add -A && git commit -m "Fix tool chip animation, gap question logic and heading, placeholder text, and start-over button" && git push origin main. Confirm commit hash and flag any assumptions.
================================================================================
