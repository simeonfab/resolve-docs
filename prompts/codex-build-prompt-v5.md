---
status: historical
---

Repo: simeonfab/ResolveOS-intake-frontend. Locate the existing public/index.html.

This replaces the subheading copy specified in the previous prompt - use this instead.

================================================================================
Screen 0 greeting beat - final subheading copy change
================================================================================

Replace the subheading text with: "Welcome to Resolve."

The word "Resolve" within that line should be styled in a slightly different colour
from the rest of the line, to stand out as the product name - use the existing amber
accent colour already used elsewhere in the app for this (do not introduce a new
colour value).

Keep everything else from the previous prompt as-is: the size hierarchy ("Hi [name]."
large, this subheading noticeably smaller), the fade-in timing/sequencing, and
ensuring it holds on a single line at both mobile and desktop widths.

================================================================================
After implementing: git add -A && git commit -m "Update screen 0 subheading copy to
Welcome to Resolve with accent colour on product name" && git push origin main.
Confirm the push succeeded and report the commit hash.
================================================================================
